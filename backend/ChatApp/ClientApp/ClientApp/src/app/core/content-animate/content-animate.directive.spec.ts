import { ContentAnimateDirective } from './content-animate.directive';

describe('ContentAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
